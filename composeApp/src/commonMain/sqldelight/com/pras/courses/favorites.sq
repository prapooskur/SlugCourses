CREATE TABLE favorites (
  id TEXT PRIMARY KEY NOT NULL
);

selectAll:
SELECT *
FROM favorites;

select:
SELECT *
FROM favorites
WHERE id = ?;

delete:
DELETE FROM favorites
WHERE id = ?;

insert:
INSERT INTO favorites(id)
VALUES (?);